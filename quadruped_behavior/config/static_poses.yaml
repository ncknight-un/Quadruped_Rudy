/**:
  ros__parameters:
    standing_pose:  
      # Input in order of leg diagram: BL, BR, FL, FR,  Head
      # Knee, Hip, Abad joints for each leg, then head joints
      # back legs first
      [1.25, 0.785, 0.0,
      -1.25, 0.785, 0.0,
       1.25, 0.785, 0.0,
      -1.25, 0.785, 0.0,
      # Head joints: looking forward
      0.25, 0.0]

    lying_pose:  
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      [0.35,-1.0, 0.0, 
      -0.35,-1.0, 0.0,
      -0.2,-1.45, 0.0, 
      0.2,-1.45, 0.0,
      # Head joints: looking forward
      0.2, 0.0]

    paw_pose:  
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      # Knee, Hip, Abad joints for each leg, then head joints
      # back legs first
      [2.0, 0.785, 0.1,
      -2.0, 0.785, -0.1,
       1.5, 0.785, 0.1,
      -1.25, -0.785, -0.1,
      # Head joints: looking forward
      0.25, 0.0]

    sitting_pose:  
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      [0.9,-0.5, 0.0, 
      -0.9,-0.5, 0.0,
      0.785,0.0, 0.0, 
      -0.785,0.0, 0.0,
      # Head joints: looking forward
      -0.1, 0.0]

    kneeling_pose:  
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      [0.785,0.0, 0.0, 
      -0.785,0.0, 0.0,
      -0.2,-1.45, 0.0, 
      0.2,-1.45, 0.0,
      # Head joints: looking forward
      1.0, 0.0]
    
    walking_pose_sequence:  
      # A sequence of poses for walking, where each pose is a vector of joint angles in the same order as above.
      # STANCE
      [1.35, 0.785, 0.0,
      # LIFT
      1.6, 0.785, 0.0,
      # SWING
      1.6, 0.5, 0.0,
      # ABDUCTION (Widen stance / stabilize)
      1.6, 0.5, 0.3,   # ← increase lateral angle
      # LAND
      1.35, 0.5, 0.0]