/**:
  ros__parameters:
    standing_pose:
      # Input in order of leg diagram: FL, BL, BR, FR,  Head
      # Knee, Hip, Abad joints for each leg, then head joints
      # back legs first
      [1.65, 1.0, 0.05,
       1.65, 1.0, 0.05,
      -1.65, 1.0, -0.05, 
      -1.65, 1.0, -0.05,
      # Head joints: looking forward
      0.1, 0.0]

    lying_pose:
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      [-0.2,-1.45, 0.0,
      0.35,-1.0, 0.0,
      -0.35,-1.0, 0.0,
      0.2,-1.45, 0.0,
      # Head joints: looking forward
      0.2, 0.0]

    paw_pose:
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      # Knee, Hip, Abad joints for each leg, then head joints
      # back legs first
      [1.5, 0.785, 0.15,
      1.785, 0.675, 0.15,
      -1.785, 0.675, -0.15,
      -1.25, -0.785, -0.15,
      # Head joints: looking forward
     -0.25, 0.0]

    sitting_pose:
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      [0.785,0.0, 0.0,
      0.9,-0.25, 0.0,
      -0.9,-0.25, 0.0,
      -0.785,0.0, 0.0,
      # Head joints: looking forward
      -0.1, 0.0]

    kneeling_pose:
      # Input in order of leg diagram: FL, BL, BR, FR, Head
      [-0.2, -1.45, 0.0,
      1.65, 0.9, 0.15,
      -1.65, 0.9, 0.15,
      0.2, -1.45, 0.0,
      # Head joints: looking forward 
      -1.0, 0.0]

    walking_pose_sequence:
      # STANCE
      [1.65, 0.9, 0.05,
      # SWING - lift and reach forward (higher hip = bent for clearance)
      1.875, 1.3, 0.05,
      # THRUST - push back (lower hip = extended, pushing back)
      1.65, 0.6, 0.05,
      # LAND - extend forward to plant (lower hip = extended reach)
      1.65, 0.7, 0.05]